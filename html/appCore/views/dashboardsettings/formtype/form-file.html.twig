<div class="dashboard__infoBlock dashboard__infoBlock--file">
    <div class="form-group">
        <label class="dashboard__label" for="{{ block.postData.block }}-{{ index }}-file">{{translate('_file_'~ form.class,'dashboardsetting')}}</label>

        <div class="dashboard__infoContainer">
            <input type="file" name="{{ form.name }}" class="form-control-file js-extra-setting js-upload-file" id="{{ form.field }}" data-block="{{ block.postData.block }}" data-types="{{ block.allowedFileMimeTypes|join(',') }}"> <!-- @TODO PEPPE: Dinamicizzare, basta che mi passi le estensioni ammesse come stringa separate da virgola. Poi converto io in array  -->
            <input type="hidden" class="form-control-file" id="{{ block.postData.block }}-{{ index }}-file-string" data-block="{{ block.postData.block }}-{{ index }}">
        </div>

        <div class="dashboard__helper">Tipi di file ammessi: {{ block.allowedFileTypes|join(', ') }}</div> <!-- @TODO PEPPE: Dinamicizzare, basta che mi passi le estensioni ammesse come stringa separate da virgola.  -->
        <div class="dashboard__error">Il file non ha una delle seguente estensioni:<br> {{ block.allowedFileTypes|join(', ') }}</div> <!-- @TODO PEPPE: Dinamicizzare, basta che mi passi le estensioni ammesse come stringa separate da virgola. -->
    </div>
</div>
