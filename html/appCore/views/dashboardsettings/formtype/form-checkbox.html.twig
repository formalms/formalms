<div class="dashboard__infoBlock dashboard__infoBlock--checkbox">
    <div class="form-group">
        <label class="dashboard__label" for="{{ block.postData.block }}-{{ index }}-file">{{translate('_checkbox_'~ form.class,'dashboardsetting')}}</label>

        <div class="dashboard__infoContainer">
            {% for key,value in form.values %}

                <div class="dashboard__el">
                    <input type="checkbox" class="checkbox" value="{{ block.postData.block }}_{{ index }}_{{ key }}" {% if block.type == type %}checked="checked"{% endif %} />{{ value }}
                </div>

            {% endfor %}
        </div>
    </div>
</div>
