<div class="js-router--dashboard dashboard">
    <script>
        window.dashboardLayoutId = '{{ dashboardLayoutId }}';
    </script>
    <div class="d-grid">
        <div class="d-row">
            {% for block in blocks %}
                {% include block.view with {'block': block } %}
            {% endfor %}
        </div>
    </div>
</div>

