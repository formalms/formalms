
<li class="green_menu {{ cart ? '' : 'hidden'}}">
 <span id="cart_element" class="badge badge-danger">{{ cart }}</span>
    <a href="index.php?r=cart/show" class="no-border-right no-before" id="cart_action" title="{{ Lang_translate('_CART', 'cart') }}">
        <div class="has-forma-tooltip">
            <span class="glyphicon glyphicon-shopping-cart top-menu__label">
                {{ Lang_translate('_CART', 'cart') }}
           </span>
        </div> 
    </a>
</li>

